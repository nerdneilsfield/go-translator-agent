package translator_tests

import (
	"testing"
)

// 测试不同preset配置下的翻译功能
func TestTranslatorWithDifferentPresets(t *testing.T) {
	// 由于需要实际的API调用，我们跳过这个测试
	t.Skip("跳过翻译器预设测试，因为需要实际的API调用")
}

// 测试LLM_client的请求和响应处理
func TestLLMClientRequestAndResponse(t *testing.T) {
	// 由于需要实际的API调用，我们跳过这个测试
	t.Skip("跳过LLM客户端请求和响应处理测试，因为需要实际的API调用")
}
